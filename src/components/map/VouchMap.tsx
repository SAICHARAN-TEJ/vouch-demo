import { useEffect,useRef,useState } from "react";
import maplibregl from "maplibre-gl"; import "maplibre-gl/dist/maplibre-gl.css"; import type { GeoPoint,RoadEvent,RoadEventType } from "@/types"; import { CHENNAI_CENTER } from "@/config/demoData"; import { ROAD_EVENT_LABEL } from "@/config/labels"; import { cn } from "@/lib/cn"; import { SchematicMap } from "./SchematicMap";
const STYLE=import.meta.env.VITE_MAP_STYLE_URL||"https://demotiles.maplibre.org/style.json"; const colors:Record<RoadEventType,string>={pothole:"bg-hazard-pothole",speed_breaker:"bg-hazard-speedbreaker",waterlogging:"bg-hazard-waterlogging",debris:"bg-hazard-debris"};
export function VouchMap({roadEvents,rider,highlightId,onSelect,className}:{roadEvents:RoadEvent[];rider?:GeoPoint|null;highlightId?:string;onSelect?:(id:string)=>void;className?:string}){const ref=useRef<HTMLDivElement>(null),mapRef=useRef<maplibregl.Map|null>(null),markers=useRef<maplibregl.Marker[]>([]);const[failed,setFailed]=useState(false),[ready,setReady]=useState(false);useEffect(()=>{if(!ref.current||failed)return;let cancelled=false;let map:maplibregl.Map;try{map=new maplibregl.Map({container:ref.current,style:STYLE,center:[CHENNAI_CENTER.longitude,CHENNAI_CENTER.latitude],zoom:12.3,attributionControl:false});}catch{setFailed(true);return;}mapRef.current=map;const timer=setTimeout(()=>{if(!cancelled)setFailed(true)},4500);map.on("error",()=>!cancelled&&setFailed(true));map.on("load",()=>{if(!cancelled){clearTimeout(timer);setReady(true)}});return()=>{cancelled=true;clearTimeout(timer);markers.current.forEach(m=>m.remove());markers.current=[];map.remove();mapRef.current=null;setReady(false)}},[failed]);useEffect(()=>{const map=mapRef.current;if(!map||!ready)return;markers.current.forEach(m=>m.remove());markers.current=[];roadEvents.forEach(ev=>{const el=document.createElement("button");el.className=cn("block rounded-full ring-2 ring-white/80 shadow-lg",colors[ev.type],ev.id===highlightId?"h-5 w-5 ring-primary animate-pulse":"h-3.5 w-3.5");el.setAttribute("aria-label",`${ROAD_EVENT_LABEL[ev.type]} details`);el.onclick=()=>onSelect?.(ev.id);markers.current.push(new maplibregl.Marker({element:el}).setLngLat([ev.longitude,ev.latitude]).addTo(map))});if(rider){const el=document.createElement("div");el.className="h-4 w-4 rounded-full bg-accent ring-4 ring-accent/30";markers.current.push(new maplibregl.Marker({element:el}).setLngLat([rider.longitude,rider.latitude]).addTo(map))}},[roadEvents,rider,highlightId,onSelect,ready]);if(failed)return <SchematicMap roadEvents={roadEvents} rider={rider} highlightId={highlightId} onSelect={onSelect} className={className}/>;return <div className={cn("relative h-full w-full",className)}><SchematicMap roadEvents={roadEvents} rider={rider} highlightId={highlightId} onSelect={onSelect} className={cn("absolute inset-0",ready&&"opacity-0")}/><div ref={ref} className={cn("absolute inset-0",ready?"opacity-100":"opacity-0")}/>{!ready&&<div className="pointer-events-none absolute right-3 top-3 rounded-full bg-bg/80 px-2.5 py-1 text-[10px] text-muted">Loading live map</div>}</div>}
