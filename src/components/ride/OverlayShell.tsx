import type { ReactNode } from "react"; import { cn } from "@/lib/cn";
export function OverlayShell({children,footer,onSkip,className,scrimClassName}:{children:ReactNode;footer?:ReactNode;onSkip?:()=>void;className?:string;scrimClassName?:string}){return <div className="absolute inset-0 z-30 flex animate-fade-in flex-col"><div className={cn("absolute inset-0 bg-bg/85 backdrop-blur-md",scrimClassName)}/><div onClick={onSkip} role={onSkip?"button":undefined} tabIndex={onSkip?0:undefined} aria-label={onSkip?"Continue":undefined} className={cn("relative z-10 flex flex-1 flex-col overflow-y-auto p-6",onSkip&&"cursor-pointer",className)}>{children}</div>{footer&&<div className="relative z-10 p-5 pt-0">{footer}</div>}</div>}
