import type { RideAnalysis } from "@/store/rideStore"; import { OverlayShell } from "./OverlayShell"; import { Icon } from "@/components/ui/Icon"; import { MANOEUVRE_LABEL } from "@/config/labels"; import { manoeuvreQuestion } from "@/lib/ui";
export function ManoeuvreOverlay({analysis,onAdvance}:{analysis:RideAnalysis;onAdvance:()=>void}){const type=analysis.result.eventType;return <OverlayShell onSkip={onAdvance} className="items-center justify-center text-center"><div className="flex flex-col items-center"><div className="relative mb-8 grid h-32 w-32 place-items-center"><span className="absolute inset-0 rounded-full bg-caution/20 animate-pulse-ring"/><span className="relative grid h-20 w-20 place-items-center rounded-full bg-caution/15"><Icon name="MoveHorizontal" className="h-9 w-9 text-caution"/></span></div><div className="mb-2 inline-flex items-center gap-1.5 rounded-full bg-caution/15 px-3 py-1 text-xs font-semibold text-caution"><Icon name="Activity" className="h-3.5 w-3.5"/>Manoeuvre detected</div><div className="text-lg font-semibold text-muted">{MANOEUVRE_LABEL[type]}</div><h2 className="mt-3 max-w-[16ch] text-3xl font-extrabold text-content">{manoeuvreQuestion(type)}</h2><p className="mt-3 max-w-[26ch] text-sm text-muted">Vouch doesn't judge the action — it looks for the context.</p></div></OverlayShell>}
